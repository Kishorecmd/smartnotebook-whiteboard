import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jaihind.whiteboard',
  appName: 'Jaihind Whiteboard',
  webDir: 'dist'
};

export default config;
